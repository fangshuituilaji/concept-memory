# coding agent外接记忆服务（concept memory）

## 使用场景
作为一个本地服务，当用户使用coding agent的时候，concept memory 可以对用户当前进行的工作目录进行扫描，生成一系列概念记忆，然后当用户通过coding agent向大模型提问时，如果是需要检索相关代码文件的任务，大模型可以调用这个本地服务，concept memory本地服务会向大模型返回一系列概念卡片的瘦身目录（概念+对应代码行索引+一行定义预览），然后大模型会再次调用同一工具传 card_ids 获取选中卡片的完整内容进行阅读。

## 接入方式
通过 MCP（Model Context Protocol）接入 coding agent。concept memory 作为 MCP server 运行在本地，暴露工具接口供 coding agent 中的大模型调用。

## 部署形式
用户不安装 Python、不装依赖、不读源码：下载一个压缩包，解压到任意目录，把包里的安装说明交给自己的 coding agent，由 agent 按说明把这个 MCP 装好，之后正常使用即可。安装说明的读者是 **agent，不是人**，因此必须写成可照着执行、可自检的步骤，而不是给人看的科普。

- **分发包**：`concept-memory-offline-win64-v<版本>.zip`，解压即用、离线可跑，发布在 GitHub Releases（账号 `fangshuituilaji`）。zip 文件名带版本，但**包内顶层文件夹名固定为 `concept-memory`**：用户升级时覆盖同名目录即可，客户端配置里的路径不用改；索引数据写在被扫描项目侧，升级不会丢。
  - `python/`：随包自带的 Python 运行时（精简运行时 + 包内 `Lib/site-packages` 里的全部依赖）。目标机器**不需要装 Python、不需要 pip、不需要联网**；不采用 venv 形态，因为 `pyvenv.cfg` 只能写绝对路径，换目录即失效。
  - `src/`：concept memory 源码，由包内启动脚本通过 `PYTHONPATH` 加载，不要求目标机再 pip install。
  - `bin/`：`memory-mcp.cmd`（MCP 服务）、`memory-web.cmd`（概念网络页面）、`memory-concepts.cmd`（命令行建索引）。启动脚本一律用 `%~dp0` 推导包根，不写死路径，也不向 stdout 输出任何文字（MCP 走 stdio）。
  - `install/INSTALL.md`：安装说明（给 agent）。
  - `install/agent-rules-template.md`：代码定位规则模板，安装时追加进用户项目的 `AGENTS.md`，让用户的 coding agent 优先调用本 MCP 而不是 grep / 整文件读。
  - `install/check_install.ps1`：开箱自检脚本，逐项校验解释器、`sqlite3`、依赖导入、文件齐全性、包版本号（`VERSION` 与源码 `__version__` 一致）与打包机路径残留。
  - `install/mcp-config-examples/`：各客户端配置片段。
  - `VERSION` / `CHANGELOG.md`：包内版本号与变更记录，自检会打印版本号，供用户报障与升级核对。
  - `.env.example`：DashScope 密钥模板；**密钥不进包**。注意 MCP 服务当前只从 MCP 客户端配置的 `env` 字段或系统环境变量读 Key，**不读 `.env`**。
- **硬约束（实测踩过，写进说明与自检）**：包内不得生成 `python311._pth`（会让解释器进隔离模式，`DLLs` 脱离 `sys.path`，`import _sqlite3` 失败，存储层直接不可用）；pip 生成的 `site-packages/bin` 启动器内嵌打包机绝对路径，打包时必须删除。
- **安装说明要覆盖的步骤**：① 定位解压后的绝对路径；② 运行自检（`check_install.ps1` 必须 PASS）；③ 按用户所用客户端写入 MCP 配置；④ 配好密钥；⑤ 验证工具可列出并能实际调用一次；⑥ 让用户在目标项目里首跑扫描；⑦ 把代码定位规则写进用户项目的 `AGENTS.md`（用包内模板）。说明里另附「升级到新版本」章节。
- **路径规则**：包内任何脚本不得写死解压路径，随包启动脚本按自身所在目录推导路径，保证解压目录改名、换盘、含空格与中文路径都能用。
- **客户端覆盖**：至少覆盖用户当前在用的 agent，以及 Claude Desktop、Cursor、Cline 的 stdio 配置样例。
- **每项目独立**：扫描数据写在**被扫描项目**的 `.concept-memory/` 下，安装目录本身不存数据，同一份安装可服务多个项目。
- **版本号口径**：唯一版本源是 `pyproject.toml` 的 `[project] version`，`src/memory_system/__init__.py` 的 `__version__` 必须与它一致（由 `tests/test_version.py` 与发布脚本双重校验），并写入包内 `VERSION`、MCP 握手返回的 serverInfo。破坏性变更（MCP 工具签名、`.concept-memory/` 数据结构、配置字段）走 MAJOR，新增能力走 MINOR，修复走 PATCH；0.x 阶段破坏性变更也走 MINOR 并在 `CHANGELOG.md` 标注 BREAKING。
- **发布链路**：`deploy/release.py` 一条命令完成「版本三处一致性校验 → CHANGELOG 段落提取 → 工作树与 tag 检查 → 跑测试 → 构建 → 离线包验收 → 生成 SHA256 → 打 tag 并推送 → 创建 GitHub Release 并上传 zip 与校验和」。支持 `--dry-run`（只出产物，不碰 git、不发网络请求）；上传走 GitHub 接口，令牌只从环境变量 `GITHUB_TOKEN` 读取，不入库、不回显；仓库不存在时可用 `--create-repo` 创建公开仓库。发行许可证为 MIT。
- **推送通道兜底**：若网络屏蔽 `github.com:443`（`git push` 连不上或 TLS 中断）而 `api.github.com` 仍可达，用 `deploy/publish_via_api.py` 走 GitHub Git Data 接口复刻一次推送（blobs → tree → commit → refs/heads → refs/tags）再建 Release；空仓库会先用 Contents 接口建一个初始提交，否则 Git Data 接口返回 409。
- **当前实现状态**：构建脚本 `deploy/build_offline_bundle.py` 已能一次跑出上述 zip（约 31MB，解压约 85MB），并支持 `--version`、按版本号命名 zip、把 `VERSION`/`CHANGELOG.md`/规则模板打进包；打包机 Python 3.11 不再写死个人路径，改为 `--runtime` > 环境变量 `CONCEPT_MEMORY_BUILD_PYTHON` > 自动探测。验收脚本 `deploy/verify_offline_bundle.py` 已在「解压到含空格与中文的新路径」下实测通过：解压 5.9 秒、包内自检 PASS（退出码 0）、真实 MCP 握手成功且 `scan_codebase`/`search_concepts` 两个工具均列出、`stderr` 零输出。尚未做的是目标机器上的完整端到端（含一次真实的在线检索返回），以及首次公开发布（需要 GitHub 令牌）。

## 服务输出
一系列有逻辑顺序的概念卡片，卡片包含概念以及对应代码行索引。直接命中的卡片在前，沿"真实使用连线"扩散出的相关卡片在后（附跳数和传播路径）。

## 目标需求
coding agent会出现文件读取占用模型上下文、模型代码定位不精准等问题，为了解决以上问题，我们需要对coding agent中模型接受的记忆做处理，让其更加精准、更加深入。

## 概念连线的唯一来源：真实使用
- 概念网络初始没有跨文件连线，只有同一个文件内的概念相互连线。
- 之后**只有真实使用才建边**：模型一次 `search_concepts` 取卡（card_ids 模式）同时取走多张卡片时，这些卡片两两之间记一条使用连线，计数 +1；重复共同使用，连线变粗（粗细有上限）。
- 连线**不由模型推断生成**：不做语义关系预测（如上游/使能等类型化关系），扫描完成后不调用任何模型建边。
- 真实使用连线为下一次检索提供参考：检索时从直接命中出发，沿使用连线按共现次数加权扩散，把历史上真正一起用过的概念带回来。

## 本地服务页面
mcp接受到工作目录后，开始加载本地记忆服务系统时，会打开一个网页页面，如果是新的项目工作目录，网页会进行初始化（显示进度）并实时生成节点，直到完整的概念网络（网络中每个节点是浅灰色的小球），每一个概念对应一个概念卡片，点击网络的某一个节点，就会出现一张概念卡片。
网站会有一个红绿按钮显示（mcp是否连接）是否连接好coding agent，绿色表明连接成功，红色表明连接失败。当连接成功时，大模型调用本地服务，被检索的节点小球会在网络中变成蓝色，同时一次检索的过程中相关概念小球会产生浅灰色连接，连接会随着调用次数变多而线条变粗（粗细有上限）。
当前已实现：同文件连线（淡色）与真实使用连线（按次数变粗）的渲染；红绿连接状态按钮（coding agent 最近调用过 MCP 工具时为绿色，超过 5 分钟无调用转红，网页自己触发的扫描不算）与检索命中节点变蓝（直接命中深蓝、扩散相关浅蓝，命中集合之间的连线同时点亮）也已实现。

## 服务过程
1. mcp服务中的提示词有一部分是告诉模型发送工作目录（以相应的格式）。
2. mcp接受到工作目录后，扫描代码生成概念卡片；默认纳入 Python 源码与 Markdown 文档（md 以标题行作为概念锚点，代码围栏内的 # 不算标题）。此时跨文件没有任何连线，仅同文件概念互连。扫描会清理掉已删除/改名文件的孤儿卡片。
3. 当用户向coding agent提问时，mcp的提示词会引导模型在需要检索文件的时候先调用 search_concepts；**检索必须在线调用 qwen-flash**：全部概念卡片的「ID+概念名+定义」目录直接放进 qwen-flash 上下文，由它挑选相关卡片并按相关性排序、只返回卡片ID，本地再按ID取回完整卡片内容。**没有本地关键词粗筛环节**，不存在中文分词导致的漏召回；卡片目录过大时按批送模型初选、合并后再终排。服务返回直接命中卡片的瘦身条目（card_id、概念名、文件行号、一行定义预览，不含源码摘录），并沿真实使用连线扩散出相关卡片。模型不可用时检索在重试后明确报错，**绝不静默回退到纯离线检索**。若后台扫描尚未完成，search_concepts 会**自动等待扫描结束**再检索，不会在半成品索引上返回结果；索引为空时明确报错提示先扫描，agent 无需自行 sleep 轮询。
4. 模型对需要的卡片再次调用 search_concepts 并传 card_ids（一次可取多张）；返回的卡片内容（概念+行索引+源码摘要）就是模型"读取"的内容。
5. 每一次多卡片取卡都会在被取卡片两两之间累加一条真实使用连线，供下一次检索扩散使用。
