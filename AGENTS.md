# 系统提示词

## 项目目标

开发一个**供 AI Agent 使用的本地外部记忆基础设施**。

## 项目具体进展和信息

`产品设计文档.md`：唯一权威来源。出现矛盾一律以它为准；任何结论与它冲突时，改结论不改文档。
- 何时读：涉及本项目目标、范围、机制、流程、接口的任务开工前必须读；纯问答可不读。
- 何时更新：本轮改动使文档中的机制、流程或「当前已实现」状态不再成立时，在同一轮内同步更新，具体改动随最终回复汇报。
- 与输出守则无关：更新文档属于工作区产物，不占用回复字数。
- `README.md`：只在用户点名，或需要了解安装、启动、使用方式时读取。

## 项目skill和MCP使用

- concept memory MCP

- 所有任务型请求先按 `intent-first` 处理。

- 意图确认后，需要落地执行、产出可见结果或建立验收标准的任务，按 `intent-to-standard` 处理。

- 可跳过对齐的只有两类：
  
  - 用户明确说「直接做」「直接改」等。
  - 同时满足以下三条的纯问答/闲聊：①不产生文件、不改动工作区、不触发有副作用的工具；②无需用户补充信息即可作答；③判断错了也没有代价。

- **兜底**：只要有任一条不满足，或无法确定属于哪一类，一律按任务处理走 `intent-first`，不得以「这看着像闲聊」为由跳过对齐。

- 跳过对齐只免除提问，不豁免风险控制：不可逆、对外发送、破坏性操作仍须单独确认。

- 优先用工具和 skill 完成任务；但 `workflow`、`ralph` 等重型/多智能体编排工具**仅限用户明确点名时使用**，不得以「多使用工具」为由自行启用。

## 模型输出守则

- 输出硬约束： 除非用户明确要求详细说明，否则最终回复必须控制在 300 个中文字符以内；超出前必须删减，不得用分点堆叠内容。输出前必须按下述口径统计。
- 字符口径：计入中文字符与中文标点；不计入英文单词、文件路径、代码、markdown 标记。
- 不受 300 字限制的例外（但仍应尽量简短）：
  - intent-first / intent-to-standard 的意图确认和标准确认消息。
  - 用户明确要求详细说明或完整输出的消息。
  - 系统指令强制要求的输出内容，例如必须列出本轮新增/修改的文件路径。
- 提问节奏：默认每轮只提一个核心问题；用户明确要求「一次多问」，或同一轮内存在多个必须同时取证的并行决策时，可批量提问。
- 根据用户水平回答：根据用户询问的内容决定输出内容的专业名词占比，用户越小白越少使用专业名词。
  
## 代码定位规则（concept memory）

- 需要定位、理解本工作区的代码时，**禁止直接 Grep/ripgrep 或整文件 Read**。

- 每次会话开始先调用 `scan_codebase` 建立该目录的概念索引；检索结果为空或连续报错时，也先重扫再检索。

- 索引就绪后一律先调用 `search_concepts` 传 query（自然语言概念或关键词，中文即可，如「使用边」「重试逻辑」）；
  它返回瘦身目录（card_id、概念名、文件行号、一行定义预览，不含源码摘录）。检索连续报错说明在线重排不可用或索引未就绪，重扫后仍失败才可回退 Grep/Read。

- 从返回结果中挑出需要的卡片，再次调用 `search_concepts` 传 `card_ids`（一次可传多张）阅读完整卡片（含按行号截取的源码摘录）。

- 只有当卡片摘录没有覆盖你需要的少数几行时，才用 Read 按行号区间补读；仍找不到线索才回退到 Grep。

- 每次多卡片取卡都在为概念网络累积真实使用连线，检索会越用越准。
